# TODO

Отложенные задачи проекта. Порядок внутри разделов — по важности.
Состояние на 2026-09-21, ветка `refactor/core`.

## Перед деплоем ветки (обязательно)

- [ ] **Выдать тенант операторам.** После деплоя аккаунт без `tenant` попадает в личную демо-песочницу,
      а не к реальным воротам. Сразу после выкладки для каждого оператора:
      `node --env-file=.env scripts/grant-tenant.mjs <телефон> shota`
      (без аргументов скрипт показывает список аккаунтов).
- [ ] **`CRON_SECRET` в Vercel.** Без него не работают оба cron: разблокировка истёкших штрафов
      и очистка простаивающих демо-песочниц.
- [ ] **Смержить `refactor/core` в `main`.** Ветка локальная, не запушена.

## База данных (отложено: «базу пока не трогаем»)

- [ ] **Уникальный индекс `{ time, number }` на коллекции `Call`.**
      Зачем: (1) единственная гарантия от дублей звонков — код делает их маловероятными, но не невозможными;
      (2) запрос «звонки за день» сейчас перебирает всю коллекцию (COLLSCAN, ~96 тыс. документов на каждый запрос).
      Как: перед созданием ещё раз проверить дубли `scripts/remove-duplicate-calls.mjs` (dry run),
      создать индекс точечной командой `createIndexes`, **не** `prisma db push` (он попытается привести к схеме
      все индексы боевой базы), затем добавить `@@unique([time, number])` в `schema.prisma`.
      Создавать вместе с деплоем новой ветки: старый код на проде не ловит ошибку вставки дубля
      и при редкой гонке ответит 500.
- [ ] **Отдельный кластер (или хотя бы отдельный пользователь БД) для `DEMO_DATABASE_URL`.**
      Сейчас демо и прод на одном кластере под одним пользователем — изоляция держится только на коде.
      Меняется только значение переменной, код не трогается.
- [ ] **Сверить лимиты Atlas** на число баз/коллекций: каждая демо-песочница — отдельная база `demo_<accountId>`,
      простаивающие удаляются через 14 дней.
- [ ] **Отдельная база для локальной разработки** (`.env.local` с другим `DATABASE_URL`) и `.env.local.example`.
      Сейчас локальный запуск под тенантом `shota` работает с боевыми данными и боевым Unitalk.

## Cloudinary (отложено)

- [ ] **Авторизация роута `POST /api/cloudinary/delete_files`.** Сейчас открыт без сессии,
      имя удаляемого ресурса захардкожено (`cars/248/...`).
- [ ] **Префикс папки по тенанту**, чтобы загрузки из демо не смешивались с реальными.
- [ ] **Переименовать `NEXT_PUBLIC_CLOUDINARY_API_SECRET`** (убрать `NEXT_PUBLIC_`) и перевыпустить секрет
      в кабинете Cloudinary. В клиентский бандл секрет сейчас не попадает (используется только в
      `app/(appLayer)/libs/cloudinary.ts`), но префикс делает утечку делом одной строки кода.

## Архитектура

- [ ] **Фаза C — независимость от БД.** Регистрация и вход через порт аккаунтов и use cases
      (сейчас Prisma и bcrypt вызываются прямо в `api/register` и `libs/authOptions.ts`);
      контрактные тесты репозиториев — один набор тестов для in-memory фейка и для Prisma-адаптера.
- [ ] **Webhook от Unitalk** («конструктор телефонных событий») как второй способ получать звонки,
      опрос истории остаётся для сверки. Сначала выяснить состав полей webhook в кабинете Unitalk.
- [ ] **Реестр тенантов без редеплоя** — сейчас новый заказчик = переменные `TENANT_<KEY>_*` в Vercel.

## Функциональность

- [ ] **Звонки в реальном времени, уровень 1:** опрос раз в 5 секунд (`refreshInterval` в `useCalls`).
      Предварительно: индекс на `Call` (см. выше), граница периода «до» должна считаться на момент запроса —
      сейчас в `CallsList`/`ViolationsList` она вычисляется один раз при загрузке модуля, и звонки,
      пришедшие после открытия страницы, не попадают в диапазон.
- [ ] **Кнопка «сбросить демо»** для посетителя песочницы.
- [ ] Кнопка «редактировать» в карточке жильца сейчас ничего не делает.

## Технический долг

- [ ] Удалить зависимость `axios` — в коде больше не импортируется.
- [ ] Обновить Next.js (сейчас 14.1.3) отдельной задачей, когда остальное устоится.
- [ ] ESLint: включить `eslint:recommended` / `next/core-web-vitals` — сейчас выключены, потому что дают
      110 / 11 ошибок в существующем UI и ломают `next build`.
- [ ] `README.md` — шаблон create-next-app, описать архитектуру (`core/`, `infrastructure/`, `contracts/`,
      `sharedLayer/api`, `sharedLayer/framework`), тенанты и демо.
- [ ] В `resolutions` закреплён `jackspeak 2.1.1` — обход бага hoisting в yarn 1; не менять без проверки
      `yarn install` + `yarn lint` + сборки bcrypt.
